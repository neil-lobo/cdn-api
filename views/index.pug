html
    head
        title test uploader
    body
        if user
            p #{user.id}
            p #{user.preferred_username}
        
        a(href="/login")
            button LOGIN
        br
        a(href="/logout")
            button LOGOUT
        br
        form(action="/upload" enctype="multipart/form-data" method="post" id="form")
            input(type="file" name="file" id="file")
            input(type="submit" name="submit" id="submit")

        p why are you here? this page doesnt even have a place to enter an api key.
        p if you want to make a simple ui for this site pm on twitch: 2547techno
        p will give credit ofc